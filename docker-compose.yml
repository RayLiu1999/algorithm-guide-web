services:
  algorithm-guide-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: algorithm-guide-web
    ports:
      - "${HOST_IP:-127.0.0.1}:${PORT:-8080}:80"
    restart: unless-stopped
    networks:
      - algorithm-network

networks:
  algorithm-network:
    driver: bridge
